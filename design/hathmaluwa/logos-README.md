The two Hathmaluwa logos, copied from the files supplied, unaltered. No vector or larger master exists.

- `hathmaluwa-horizontal.png` (210×64, opaque): the mark, the Sinhala name "හත්මාළුව" and the Latin name "HATHMALUWA" on a `plate` (#f2f2f2) rounded panel with a white margin. Use it in headers and footers on `surface` in the light theme.
- `hathmaluwa-stacked.png` (170×153, transparent): the mark above the "Hathmaluwa" wordmark in navy (#1a5995). Use it for square or tall spaces on `surface` or `plate` in the light theme.

Both are made for white. In the dark theme, place either on a `logo-ground` panel: the horizontal file has opaque white corners, and the stacked wordmark's navy is not legible on dark surfaces.

Both are raster images with a soft gloss on the mark. They are shown through `<img>` only, so no ink can be inherited. Show them at or below their pixel size.
